/** @type {import('next').NextConfig} */
const nextConfig = {
    images: {
        domains: ['i.gr-assets.com'], // Add your remote image source hostname(s) here
      },
}

module.exports = nextConfig
